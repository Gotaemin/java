package dao;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;

import vo.Movie;
import static db.JdbcUtil.*;

public class MovieDAO {
	Connection conn = null;
	
	public void setConnection(Connection conn) {
		this.conn = conn;
	}

	public ArrayList<Movie> selectMovieList() {
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		
		ArrayList<Movie> movieList = null;
		
		try {
			pstmt = conn.prepareStatement("select * from movielist");
			rs = pstmt.executeQuery();
			
			if(rs.next()) {
				movieList = new ArrayList<Movie>();
				
				do {
					movieList.add(new Movie(rs.getString("title"),
											rs.getInt("visitors"),
											rs.getString("actor"),
											rs.getString("image")));
				}while(rs.next());
			}
		}catch(SQLException e) {
			e.printStackTrace();
		}finally {
			close(pstmt);
			close(rs);
		}
		
		return movieList;
	}
	
	public ArrayList<Movie> selectMovie(String inputTitle) {
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		
		ArrayList<Movie> movieList = null;
		Movie movie = null;
		
		try {
			movieList = new ArrayList<Movie>();
			pstmt = conn.prepareStatement("select * from movielist where title=?");
			pstmt.setString(1,inputTitle);
			rs = pstmt.executeQuery();
			
			if(rs.next()) {
				movie = new Movie(rs.getString("title"),rs.getInt("visitors"),rs.getString("actor"),rs.getString("image"));
			}
			
			//System.out.println(movie.getTitle() + movie.getVisitors() + rs.getString("actor"));
		
			movieList.add(movie);
			
		}catch(SQLException e) {
			e.printStackTrace();
		}finally {
			close(pstmt);
			close(rs);
		}
		
		return movieList;
	}

	public ArrayList<Movie> setAlignmentMovie(String alignment) {
		PreparedStatement pstmt = null;
		ResultSet rs = null;
		ArrayList<Movie> movieList = new ArrayList<Movie>();
		Movie movie = null;
		
		try {
			String sql = "select * from movielist order by ? desc";
			
			pstmt = conn.prepareStatement(sql);
			pstmt.setString(1, alignment);
			rs = pstmt.executeQuery();
			
			while(rs.next())
			{			
				movie = new Movie(rs.getString("title"), rs.getInt("visitors"), rs.getString("actor"), rs.getString("image"));
				movieList.add(movie);
				//System.out.println(movie.getTitle() + movie.getVisitors() + rs.getString("actor"));
			}
			
			for (Movie movie2 : movieList) {
				System.out.println(movie2.getTitle()+movie2.getActor()+movie2.getVisitors());
			}
		}catch(Exception e) {
			e.printStackTrace();
		}finally {
			close(pstmt);
			close(rs);
		}
		
		return null;
	}

}
